---
- name: Validate and reload sshd
  ansible.builtin.command: /usr/sbin/sshd -t
  changed_when: true
  notify: Reload sshd

- name: Reload sshd
  ansible.builtin.systemd_service:
    name: "{{ system_ssh_service }}"
    state: reloaded
